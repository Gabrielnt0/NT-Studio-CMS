# Formação — instalação

Execute no Supabase SQL Editor, nesta ordem:

1. `Education - Initial Schema`
2. `Education - Grant Auth Permissions`
3. `Education - Reload Schema`

Os scripts estão em `supabase/education/`.

Depois execute:

```bash
npm run build
npm run dev
```
